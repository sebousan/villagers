_merge: deep
admin:

  # Config
  cms: pagescms
  nested:
    depth: 0
  auth:
    netlify_identity: false

  # Collections
  collections:
    indexes:
      enable: false

  # Blocks
  blocks:
    enable:
      - cta
      - form
      - paragraph