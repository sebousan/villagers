_merge: deep
admin:

  # Config
  cms: pagescms
  nested:
    depth: 0
  auth:
    netlify_identity: false

  # Collections
  collections:
    indexes:
      enable: false
    config:
      files:
        - nav-primary
        - nav-legal
        - nav-social 
        - footer 
        - seo

  # Blocks
  blocks:
    enable:
      - cta
      - form
      - paragraph
    paragraph:
      - surtitle
      - title
      - text_markdown
  
  # Fields
  fields:
    hero:
      - title
      - text_markdown